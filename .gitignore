# build output
dist/
.astro/

# dependencies
node_modules/

# local toolchain (font subsetting only — outputs are committed)
.venv/

# environment / secrets — nothing private ever enters this repo (§9.2)
.env
.env.*
!.env.example

# Example/template articles and their assets — kept locally as references
# while writing, never tracked or deployed to production.
notes/example-*.md
images/stages.svg

# macOS
.DS_Store
